# Conformance

An implementation is conformant at level `vN` when it consumes the corresponding vector
and returns the expected result for every case.

Passing `./run.sh` is internal consistency only. Independent implementation against
`vectors/*.json` is interop.
